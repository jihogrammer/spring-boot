plugins {
    id "java"
    id "org.springframework.boot" version "3.2.3"
    id "io.spring.dependency-management" version "1.1.4"
}

group = "dev.jihogrammer"
version = "0.0.1"

java {
    sourceCompatibility = "17"
}

configurations {
    compileOnly {
        extendsFrom annotationProcessor
    }
}

repositories {
    mavenCentral()
}

dependencies {
    implementation("org.springframework.boot:spring-boot-starter-web")
    implementation("org.springframework.boot:spring-boot-starter-thymeleaf")
    implementation("org.springframework.boot:spring-boot-starter-validation")
    implementation("org.springframework.boot:spring-boot-starter-aop")

    implementation(project(":domain:members"))
    implementation(project(":web:core"))

    compileOnly("org.projectlombok:lombok")
    annotationProcessor("org.projectlombok:lombok")
}

tasks.register('sign-up') {
    exec {
        executable "curl"
        args "-i", "--no-progress-meter",
                "-X", "POST", "http://localhost:8080/members/sign-up",
                "-H", "Connection: close",
                "-H", "Content-Type: application/x-www-form-urlencoded",
                "-d", "username=jihogrammer&password=1234"
    }
}

tasks.register('sign-in') {
    exec {
        executable "curl"
        args "-i", "--no-progress-meter",
                "-X", "POST", "http://localhost:8080/members/sign-in",
                "-H", "Connection: close",
                "-H", "Content-Type: application/x-www-form-urlencoded",
                "-d", "username=jihogrammer&password=1234"
    }
}
