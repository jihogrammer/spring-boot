server:
  port: 7777

spring:
  profiles:
    active: local
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:item;DB_CLOSE_DELAY=-1
    username: sa
    schema: classpath:sql/schema.sql
    data: classpath:sql/data.sql
    initialization-mode: always
